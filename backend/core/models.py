# from djongo import models

# class User(models.Model):
#     # this will live in MongoDB as a document in the "core_user" collection
#     name  = models.CharField(max_length=100)
#     email = models.EmailField(unique=True)

#     def __str__(self):
#         return self.name
